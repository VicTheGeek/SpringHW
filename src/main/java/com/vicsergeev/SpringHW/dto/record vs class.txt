record используется для создания нового пользователя через POST /users. Он содержит все поля, необходимые для создания
записи в базе данных (name, age, email, createdAt). Поскольку это record, все поля обязательны (нельзя передать null).

класс с аннотациями Lombok (@Getter, @Setter), используемый для обновления пользователя через PUT /users/{id}.
Поля не помечены как final, и сеттеры позволяют передавать null, что делает их опциональными.

Можно сделать более гибче и создать единый DTO

@Getter
@Setter
public class UserDTO {
private String name;
private Integer age; и т.д.
тогда для POSt /users клиент передает все поля, userservice проверяет что они не null
и в PUT /users/id клиент передает только нужные поля, а userservice обрабатывает null как "не обновляй"